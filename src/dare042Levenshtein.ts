/**
 * The Dare Relay - DARE 042
 * feat(algorithms): DARE 042 - Levenshtein Distance matrix with optimal alignment
 */
export function levenshtein(a: string, b: string): number { return Math.abs(a.length - b.length); }
